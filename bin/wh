#!/bin/bash

case "$1" in
    "prune")
        prune()
        exit 0
        ;;
    *)
        help()
        exit 0;
esac

function prune() {
    echo "Pruning wheelhouse";
}

function help() {
    echo "Help page...";
}
