name: fis
root: ~/j/level12/fis

windows:
    - FIS:
        layout: main-vertical
        panes:
          - cd fis-residuals && mktmpenv -q && pipiwheel -q -r requirements/dev-env.txt && pip install -q -e .

    - KEG:
        layout: main-vertical
        panes:
          - cd ../../oss/keg && workon keg

    - KEG-ELEM:
        layout: main-vertical
        panes:
          - cd ../../oss/keg-elem

    - TRIB:
        layout: main-vertical
        panes:
          - cd ../../oss/tribune

