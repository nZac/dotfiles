# ~/.tmuxinator/dotfiles.yml

name: personal
root: ~/

windows:
  - DOTFILES:
      layout: main-vertical
      panes:
        - cd .dotfiles

  - BLOG:
      layout: main-vertical
      panes:
        - cd j/blog

  - COMPUTE:
      layout: main-vertical
      panes:
        - cd j/compute
