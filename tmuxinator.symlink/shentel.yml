name: shentel
root: ~/j/level12

windows:
    - SHENTEL:
        layout: main-vertical
        panes:
          - cd shentel-enrollment && workon shentel-enrollment

    - A-ROLES:
        layout: main-vertical
        panes:
          - cd ansible-roles

    - A-PLAYS:
        layout: main-vertical
        panes:
          - cd ansible-playbooks

    - A-VAGRANT:
        layout: main-vertical
        panes:
          - cd ansible-vagrant
